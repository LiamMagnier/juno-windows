/** Placeholder — replaced by the Code mode build. */
export function CodeView() {
  return (
    <div
      style={{
        flex: 1,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "hsl(var(--muted-foreground))",
      }}
    >
      Code mode isn't available yet.
    </div>
  );
}
