/** Placeholder — replaced by the chat feature build. */
export function ChatView() {
  return (
    <div
      style={{
        flex: 1,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "hsl(var(--muted-foreground))",
      }}
    >
      Loading…
    </div>
  );
}
