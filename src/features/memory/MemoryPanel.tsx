/** Placeholder — replaced by the memory feature build. */
export function MemoryPanel() {
  return (
    <div
      style={{
        flex: 1,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "hsl(var(--muted-foreground))",
      }}
    >
      Loading…
    </div>
  );
}
