/**
 * Placeholder slot for the Code-mode sidebar, which ships as a separate
 * feature. Intentionally renders nothing yet.
 */
export function CodeSidebarSlot() {
  return <div className="sidebar-content" />;
}
