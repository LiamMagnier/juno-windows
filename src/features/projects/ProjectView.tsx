/** Placeholder — replaced by the projects feature build. */
export function ProjectView({ projectId: _projectId }: { projectId: string }) {
  return (
    <div
      style={{
        flex: 1,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "hsl(var(--muted-foreground))",
      }}
    >
      Loading…
    </div>
  );
}
