/** Placeholder — replaced by the connectors feature build. */
export function ConnectorsPanel() {
  return (
    <div
      style={{
        flex: 1,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "hsl(var(--muted-foreground))",
      }}
    >
      Loading…
    </div>
  );
}
